#ifndef DATABASE_SCHEME_H
#define DATABASE_SCHEME_H

#include <vector>
#include <string>

using namespace std;

class HScheme: public vector<string> {
    public:
        HScheme(){};
        ~HScheme(){};
};

#endif